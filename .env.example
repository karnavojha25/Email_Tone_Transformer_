# Optional: if set, the app falls back to this key when a user doesn't paste their own.
# Never commit your real .env file. Use a real placeholder here, never an actual key.
GEMINI_API_KEY=your_key_here
